%h2
  All Picasa Albums
%table.table.table-bordered.table-striped
  %tr
    %th Album
  - @albums.each do |album|
    %tr
      %td 
        = link_to album[:title], album_photos_path(album_id: album[:id])

